default_platform(:ios)

platform :ios do
  desc "Build and upload to TestApp.io"
  lane :beta do
    build_app(scheme: "TestCiApp") # استبدل TestCiApp باسم الـ Scheme بتاعك

    upload_to_testappio(
      api_token: ENV["TESTAPPIO_API_TOKEN"]
    )
  end
end
